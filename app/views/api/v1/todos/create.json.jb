{
  id:          @todo.id,
  task:        @todo.task,
  description: @todo.description,
  status:      @todo.status,
  userId:      @todo.user_id,
  createdAt:   @todo.created_at,
  updatedAt:   @todo.updated_at
}
