components:
  schemas:
    Todo:
      title: Todo
      type: object
      properties:
        id:
          type: integer
        task:
          type: string
        description:
          type: string
          nullable: true
        status:
          type: string
          enum: [pending, in_progress, completed]
        userId:
          type: integer
        estimateStartAt:
          type: string
          format: date
          nullable: true
          example: "2026-06-11"
        estimateEndAt:
          type: string
          format: date
          nullable: true
          example: "2026-06-15"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required: [id, task, status, userId, createdAt, updatedAt]
